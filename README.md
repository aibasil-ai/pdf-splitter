# PDF 智慧切割器 (單檔版)

一個完全在瀏覽器內運行的 PDF 切割工具，結合 Google Gemini AI 提供智慧章節建議與內容摘要功能。

---

## ✨ 功能特色

### 🔪 PDF 切割功能
| 功能 | 說明 |
|------|------|
| **範圍切割** | 指定起始頁與結束頁，提取連續頁面為單一 PDF |
| **獨立頁面切割** | 輸入多個頁碼（逗號分隔），分別切割並打包成 ZIP |

### 🤖 AI 智慧功能
| 功能 | 說明 |
|------|------|
| **AI 摘要指定頁面** | 支援單頁 (`1, 3, 5`) 或連續頁 (`1-5`) 格式，摘要指定頁面的完整內容 |

### 🔒 隱私保護
- **完全本地處理**：PDF 切割操作在瀏覽器內完成，檔案不會上傳至伺服器
- **AI 功能僅傳輸文字**：僅將提取的文字片段傳送至 Google Gemini API

---

## 🛠️ 技術架構

### 前端技術棧
| 技術 | 版本 | 用途 |
|------|------|------|
| React | 18 | UI 框架（透過 CDN 載入） |
| Tailwind CSS | CDN | 樣式框架 |
| Babel Standalone | - | 瀏覽器端 JSX 編譯 |

### PDF 處理函式庫
| 函式庫 | 版本 | 用途 |
|--------|------|------|
| [pdf-lib](https://pdf-lib.js.org/) | 1.17.1 | PDF 讀取、頁面複製、建立新 PDF |
| [PDF.js](https://mozilla.github.io/pdf.js/) | 3.11.174 | 文字提取（供 AI 分析） |
| [JSZip](https://stuk.github.io/jszip/) | 3.10.1 | 多頁面打包為 ZIP |

### AI 整合
| 服務 | 用途 |
|------|------|
| Google Gemini API | 章節偵測、內容摘要 |

---

## 📖 程式碼架構

### 狀態管理 (React Hooks)
```javascript
// 核心狀態
const [pdfFile, setPdfFile] = useState(null);     // 原始 PDF 檔案
const [pdfDoc, setPdfDoc] = useState(null);       // pdf-lib 文件物件
const [pageCount, setPageCount] = useState(0);    // 總頁數
const [mode, setMode] = useState('range');        // 模式：'range' | 'individual' | 'summary'

// 輸入狀態
const [rangeStart, setRangeStart] = useState(''); // 範圍起始頁
const [rangeEnd, setRangeEnd] = useState('');     // 範圍結束頁
const [individualPages, setIndividualPages] = useState(''); // 獨立頁碼

// AI 相關
const [userApiKey, setUserApiKey] = useState('');     // Gemini API Key
const [selectedModel, setSelectedModel] = useState('gemini-2.5-flash'); // 模型選擇
const [aiMsg, setAiMsg] = useState('');               // AI 回應訊息
const [summaryPages, setSummaryPages] = useState(''); // 摘要頁碼輸入

// 處理狀態
const [isProcessing, setIsProcessing] = useState(false);   // PDF 處理中
const [isAiProcessing, setIsAiProcessing] = useState(false); // AI 處理中
const [statusMsg, setStatusMsg] = useState({ type: '', text: '' }); // 狀態訊息
```

---

### 核心函數說明

#### 1. `processFile(file)` - PDF 載入
```
流程：File → ArrayBuffer → PDFDocument.load() → 更新狀態
```
- 使用 `pdf-lib` 載入 PDF
- 取得總頁數並初始化輸入欄位

#### 2. `extractTextByPages(pageNumbers)` - 指定頁碼文字提取
```
用途：根據指定頁碼陣列提取完整文字（供摘要使用）
參數：pageNumbers - 頁碼陣列（可不連續）
輸出：格式化的完整文字字串 "[Page X]:\n完整內容\n\n"
```
- 使用 `PDF.js` 提取文字內容
- 取整頁所有文字（無字數限制）

#### 3. `parsePageInput(input)` - 頁碼解析（內嵌於 handleAiSummarize）
```
用途：解析使用者輸入的頁碼字串
支援格式：
  - 單頁："1, 3, 5" → [1, 3, 5]
  - 連續頁："1-5" → [1, 2, 3, 4, 5]
  - 混合："1-3, 7, 10-12" → [1, 2, 3, 7, 10, 11, 12]
```

#### 4. `handleAiSummarize()` - AI 內容摘要
```
流程：
1. 使用 parsePageInput 解析頁碼輸入
2. 檢查頁數限制（最多 10 頁，超過則提示使用者）
3. 使用 extractTextByPages 提取完整文字
4. 傳送至 Gemini API 請求摘要
5. 顯示摘要結果
```

#### 5. `handleSplit()` - 執行切割
```
範圍模式 (range)：
  1. 建立新 PDFDocument
  2. 複製指定頁面範圍
  3. 儲存並下載為 PDF

獨立頁面模式 (individual)：
  1. 解析頁碼字串，去重並排序
  2. 為每個頁碼建立獨立 PDF
  3. 使用 JSZip 打包
  4. 下載 ZIP 檔案
```

#### 6. `downloadBlob(data, name, mimeType)` - 檔案下載
```
流程：Blob → URL.createObjectURL → 觸發下載 → 清理 URL
```

---

## 🎨 UI 元件結構

```
PDFSplitter (主元件)
├── Header (標題列)
│   ├── 設定按鈕 (切換 API Key 面板)
│   └── 重新上傳按鈕
│
├── API Key 設定面板 (可收合)
│   ├── API Key 輸入框
│   └── 模型選擇下拉選單
│
├── 主內容區
│   ├── [未上傳] 拖放上傳區
│   └── [已上傳] 切割控制面板
│       ├── 檔案資訊卡
│       ├── 模式切換標籤 (三個 Tab)
│       │   ├── 範圍切割
│       │   ├── 獨立頁面
│       │   └── AI 摘要 (紫色)
│       ├── 輸入區域
│       │   ├── [範圍模式] 起始頁/結束頁
│       │   ├── [獨立模式] 頁碼輸入
│       │   └── [摘要模式] 頁碼輸入 + 執行摘要按鈕
│       ├── AI 回應區域
│       ├── 狀態訊息 (成功/錯誤)
│       └── 下載按鈕 (僅在切割模式顯示)
│
└── 頁尾 (隱私說明)
```

---

## 📊 支援的 Gemini 模型

| 模型 | API 名稱 | 說明 |
|------|----------|------|
| Gemini 2.5 Flash | `gemini-2.5-flash` | **推薦**，穩定且快速 |
| Gemini 2.5 Pro | `gemini-2.5-pro` | 穩定，強大推理能力 |
| Gemini 3 Flash Preview | `gemini-3-flash-preview` | 最新，快速 |
| Gemini 3 Pro Preview | `gemini-3-pro-preview` | 最新，進階推理 |
| Gemini 2.0 Flash | `gemini-2.0-flash` | 穩定，高效 |

---

## 🔧 API 呼叫格式

### Gemini API 請求
```javascript
fetch(`https://generativelanguage.googleapis.com/v1beta/models/${selectedModel}:generateContent?key=${apiKey}`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
        contents: [{
            parts: [{ text: prompt }]
        }]
    })
});
```

### 回應解析
```javascript
const aiText = data.candidates?.[0]?.content?.parts?.[0]?.text;
```

---

## ⚙️ 限制與注意事項

| 項目 | 限制 |
|------|------|
| AI 摘要 | 支援指定頁碼（單頁如 `1, 3, 5` 或連續頁如 `1-5`），最多 10 頁，取整頁完整文字 |
| 受密碼保護 PDF | 無法處理 |
| 損壞的 PDF | 無法處理 |
| API Key | 使用 AI 功能必須提供有效的 Gemini API Key |

---

## 🚀 使用方式

1. 直接在瀏覽器開啟 `pdf smart splitter.html`
2. （可選）點擊「設定 Key」輸入 Gemini API Key 以啟用 AI 功能
3. 上傳 PDF 檔案
4. 選擇切割模式：
   - **範圍切割**：輸入起始頁與結束頁
   - **獨立頁面**：輸入頁碼（逗號分隔）或點擊「AI 自動建議」
5. 點擊下載按鈕取得切割後的檔案

---

## 📝 授權

本專案採用 MIT 授權。
