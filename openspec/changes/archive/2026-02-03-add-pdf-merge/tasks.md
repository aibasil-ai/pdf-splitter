## 1. 狀態與共用工具

- [x] 1.1 在 `index.html` 新增合併模式的 state（多檔檔案陣列、選頁輸入）
- [x] 1.2 抽出共用頁碼解析函式（支援逗號與範圍、保序去重）

## 2. UI 與模式切換

- [x] 2.1 在模式 Tabs 新增「多檔合併」與「選頁合併」選項與圖示
- [x] 2.2 建立多檔合併上傳區（`multiple` 檔案輸入）與檔案順序列表/清空控制
- [x] 2.3 建立單檔選頁合併輸入區（頁碼提示與範例）

## 3. 核心合併流程

- [x] 3.1 實作多檔合併：逐檔載入 PDF、依順序複製頁面並輸出單一 PDF
- [x] 3.2 實作多檔合併的驗證與錯誤訊息（空輸入、非 PDF 檔案）
- [x] 3.3 實作選頁合併：驗證頁碼範圍、依輸入順序合併並輸出單一 PDF

## 4. 狀態回饋與驗證

- [x] 4.1 更新合併流程的成功/失敗訊息與下載檔名規則
- [ ] 4.2 手動驗證：多檔合併順序、選頁合併輸出、錯誤情境提示
